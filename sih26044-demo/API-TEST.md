# API quick test

With the server running, these endpoints are available:

- `GET /api/health` — health check
- `GET /api/skills` — skill catalog
- `GET /api/opportunities` — opportunities
- `POST /api/match` — calculate student-to-opportunity matches
- `POST /api/roles` — publish an industry opportunity
- `/docs` — interactive Swagger API documentation
